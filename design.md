# 서울 지역 맞추기 퀴즈 — 서비스 디자인

## 1. 제품 목표

서울특별시 25개 자치구의 위치와 이름을 1~2분짜리 퀴즈로 익히게 한다. 한 세션의 목표는 **5문제**이며, 학습 부담보다 “한 문제만 더”라는 가벼운 완주감을 우선한다.

## 2. 듀오링고에서 가져온 핵심 원칙

- **행동으로 학습:** 지도에서 구역이 먼저 빛나고, 사용자가 이름을 떠올린 뒤 답한다. 설명보다 회상(recall)이 우선이다.
- **짧은 루프와 즉시 피드백:** 정답/오답을 누르는 즉시 지도, 정답명, 짧은 위치 힌트를 보여준다.
- **가시적 진척:** 매 문제 XP, 상단 진행 바(예: `3 / 5`), 일일 목표를 동시에 표시한다.
- **습관 형성:** 하루 1세트 완료 시 스트릭을 1일 연장한다. 놓친 날은 주 1회 ‘스트릭 보호권’으로 부담을 줄인다.
- **선택적 경쟁:** 주간 XP 순위는 별도 탭과 opt-in으로 제공한다. 기본 학습 흐름을 방해하지 않는다.

듀오링고는 짧은 레슨, XP·리더보드, 스트릭, 보석, 오답 뒤 연습을 결합해 재방문을 유도한다. 이 앱은 그 구조를 지리 학습에 맞게 단순화한다. [Duolingo 학습 안내](https://blog.duolingo.com/duolingo-101-how-to-learn-a-language-on-duolingo/), [Duolingo 교수법](https://blog.duolingo.com/duolingo-teaching-method/)

## 3. 핵심 경험과 화면

1. **홈:** “오늘의 서울 5문제” CTA, 현재 스트릭 🔥, 오늘 XP와 다음 배지 진행률.
2. **퀴즈:** 상단에 닫기·하트 3개·진행 바. 중앙에는 인터랙티브 서울 SVG 지도, 활성 구는 높은 대비의 라임색으로 하이라이트한다.
3. **주관식:** `이 지역구의 이름은?` 질문 아래 한글 입력창과 `확인` 버튼. `동대문구`처럼 ‘구’ 생략 입력도 허용한다.
4. **객관식:** 질문 아래 2×2 큰 답안 버튼. 보기에는 서로 인접하거나 혼동하기 쉬운 구를 의도적으로 섞는다.
5. **결과/복습:** 정답은 초록, 오답은 산호색으로 지도와 답안을 표시한다. “서초구는 서울 남동부”처럼 한 줄 단서를 제공하고 `계속하기`로 즉시 다음 문제로 이동한다.
6. **완료:** 획득 XP, 정확도, 새 배지/스트릭 축하 애니메이션, `오답만 복습`과 `한 세트 더` CTA.

## 4. 게임 규칙

| 요소 | 규칙 |
| --- | --- |
| XP | 정답 +10, 3연속 정답부터 콤보 +5 |
| 하트 | 오답 시 1개 차감; 0개면 오답 구 3문제 복습 후 1개 회복 |
| 스트릭 | 하루 한 세트 완료로 연장 |
| 배지 | 25구 첫 완주, 권역별 완주, 90% 정확도 |
| 난이도 | 초급: 전체 구 이름 표시 → 중급: 객관식 → 고급: 주관식·시간 제한 |

## 5. 비주얼 시스템

밝고 낙관적인 교육용 게임 톤을 사용하되, 듀오링고의 자산·캐릭터·정확한 UI는 복제하지 않는다.

- **색상:** 잉크 `#1F2937`, 배경 `#F8FAFC`, 정답/주요 행동 `#58CC02`, 오답 `#FF6B6B`, XP `#FFC800`, 지도 기본선 `#94A3B8`, 비활성 구 `#E2E8F0`.
- **타이포:** Pretendard 또는 system sans-serif; 제목 28–32px/800, 질문 22px/700, 본문 16px/500. 한글 줄바꿈을 자연스럽게 유지한다.
- **형태:** 버튼과 카드에 16px 둥근 모서리, 버튼 하단 3px 진한 그림자로 눌림 감각을 만든다. 44px 이상의 터치 영역을 보장한다.
- **모션:** 정답 시 180ms 팝·콘페티, 오답 시 180ms 가벼운 좌우 흔들림. `prefers-reduced-motion`에서는 모션을 제거한다.

## 6. 접근성 및 지도 상호작용

색만으로 정오답을 구분하지 말고 아이콘과 텍스트를 병기한다. SVG 각 구는 실제 `<button>` 또는 `role="button"`, 구 이름의 `aria-label`, 키보드 포커스 링을 제공한다. 모바일에서는 지도를 고정 비율로 표시하고 확대/축소 및 구 선택을 지원한다.

## 7. 성공 지표

첫 세트 완주율, 7일 스트릭 비율, 오답 구 재시도율, 구별 정답률을 추적한다. 목표는 빠른 정답이 아니라 반복 후 정확도가 높아지는 학습이다.
