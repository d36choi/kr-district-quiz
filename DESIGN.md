# Seoul Geo-Quiz Design System

> 서울 25개 자치구를 짧고 즐거운 반복 학습으로 익히는 모바일 미니게임의 제품·UI/UX 정본이다.

## 1. 문서 역할과 우선순위

이 문서는 제품 경험과 시각 방향을 정의한다. 구현 시 충돌이 생기면 아래 순서를 따른다.

1. 앱인토스 공식 문서와 플랫폼 제약
2. `seoul-district-quiz/docs/design-guide.md`의 하드 규칙
3. 이 문서의 제품·화면 원칙
4. `seoul-district-quiz/src/styles/tokens.css`의 실제 구현값

토큰 구현값이 이 문서와 다르면 디자인 변경 작업에서 토큰 파일을 함께 갱신한다. 듀오링고의 짧은 학습 루프와 즉시 피드백은 참고하되, 색상·캐릭터·자산·화면 구성을 복제하지 않는다.

## 2. 제품 목표

- 사용자가 1~2분 안에 서울 자치구 5문제를 완주한다.
- 설명보다 지도를 먼저 보고 이름을 떠올리는 회상 학습을 우선한다.
- 오답은 실패가 아니라 짧은 위치 단서와 재도전으로 이어지는 학습 재료로 다룬다.
- 진척, 수집, 연속 학습은 동기를 보조하되 불안이나 결제를 유도하지 않는다.

핵심 감정은 “가볍게 시작하고, 바로 반응을 받고, 서울 지도가 조금씩 내 것이 되는 느낌”이다.

## 3. 경험 원칙

### 짧은 집중 루프

한 화면에는 한 문제와 한 가지 주요 행동만 둔다. 질문, 시각 자료, 답안, 확인 순서가 한눈에 읽혀야 한다.

### 즉각적이지만 부담 없는 피드백

정답은 선명하게 축하하고, 오답은 정답명과 한 줄 위치 단서를 바로 알려준다. 오답 문구는 비난하거나 감정을 단정하지 않는다.

### 지도가 곧 진척도

25개 자치구를 단순 목록이 아니라 수집 가능한 지도 조각으로 표현한다. 마스터한 구는 색이 채워지고, 아직 학습하지 않은 구는 중립색으로 남는다.

### 선택적 경쟁

주간 리그는 별도 화면에서 사용자가 선택해 참여한다. 기본 퀴즈 흐름에는 순위 압박이나 강제 공유를 넣지 않는다.

## 4. 브랜드와 비주얼 아이덴티티

### 아트 디렉션

서울의 단청색과 한강색을 현대적으로 톤다운한 플랫 그래픽을 사용한다. 흰색과 따뜻한 회백색 바탕 위에 제한된 강조색을 쓰고, 모든 카드와 버튼은 둥근 실루엣으로 통일한다.

화면은 교육 앱보다 캐주얼 퍼즐 게임에 가까워야 한다. 불필요한 장식, 과한 그라디언트, 상시 카드 그림자는 사용하지 않는다.

### 컬러 팔레트

| 역할 | 토큰 | 기준값 | 용도 |
| --- | --- | --- | --- |
| 단청 레드 | `--brand-primary` | `#A9433D` | 주요 CTA, 선택 강조, 브랜드 포인트 |
| 단청 소프트 | `--brand-soft` | `#FBECEA` | 선택 배경, 약한 강조 |
| 한강 블루 | `--color-river` | `#3D7C8A` | 한강, 보조 정보, 지도 줌 컨트롤 |
| 성공 그린 | `--color-success` | `#55D84B` | 정답, 완료, 해금 |
| 성공 딥 | `--color-success-deep` | `#287A2C` | 성공 텍스트와 테두리 |
| 오류 레드 | `--color-error` | `#EF6461` | 오답, 하트 차감 |
| 오류 딥 | `--color-error-deep` | `#A4383D` | 오답 텍스트와 테두리 |
| XP 골드 | `--color-xp` | `#F2B84B` | XP, 콤보, 리그 보상 |
| 강한 텍스트 | `--color-text-strong` | `#1F2937` | 제목과 핵심 수치 |
| 기본 배경 | `--color-bg` | `#FFFFFF` | 카드와 입력 표면 |
| 캔버스 배경 | `--color-bg-canvas` | `#F7F5F0` | 전체 화면 |
| 비활성 지도 | `--color-map-idle` | `#DCE2E7` | 아직 학습하지 않은 자치구 |
| 지도 경계 | `--color-map-border` | `#8794A1` | 자치구 경계 |

단청 레드와 한강 블루는 토스 브랜드 컬러와 명확히 구별한다. 성공 그린은 듀오링고의 정확한 색상값을 복제하지 않는다. 상태는 색만으로 전달하지 않고 아이콘과 문구를 함께 사용한다.

### 타이포그래피

- 본문은 시스템 산세리프를 사용한다. Toss Product Sans는 사용하지 않는다.
- Display `32px/700`, Title `24px/700`, Subtitle `17px/600`, Body `17px/400`, Body Small `15px/400`, Caption `13px/400`의 제한된 스케일을 쓴다.
- `font-weight`는 400~700만 사용한다. 본문은 최소 15px, 모든 텍스트는 12px 이상을 유지한다.
- 한국어는 `word-break: keep-all`과 긴 문자열을 위한 `overflow-wrap: break-word`를 적용한다.
- 이모지는 Tossface로 일관되게 렌더링한다.

### 형태와 깊이

- 카드와 팝업은 18~24px, 버튼과 입력은 14~18px의 둥근 모서리를 기본으로 한다.
- 주요 버튼은 눌림 상태에서 아래쪽 깊이가 줄어드는 물리적 반응을 준다.
- 일반 카드에는 그림자를 사용하지 않고 테두리나 배경 차이로 구분한다.
- 지도·로드뷰처럼 실제로 떠 있는 미디어 뷰어와 바텀 피드백 패널에만 매우 옅은 그림자를 허용한다.
- 모든 터치 대상은 최소 44×44 CSS px을 확보한다.

## 5. 마스코트 시스템

서울의 새인 까치를 모티프로 한 독자 캐릭터를 사용한다. 서울시 공식 해치 캐릭터나 타 서비스 캐릭터의 형태·표정·색 배치를 복제하지 않는다.

마스코트는 작은 검정·아이보리 몸체와 단청 레드 꼬리 포인트를 가진 단순한 플랫 형태로 설계한다. 사용 목적은 다음 네 가지로 제한한다.

- **기본:** 퀴즈 시작을 안내한다.
- **정답:** 날개를 들고 가볍게 뛰어오른다.
- **오답:** 놀란 표정 뒤 위치 단서를 가리킨다.
- **완료:** 스트릭 불꽃이나 새로 해금한 구 배지를 함께 보여준다.

마스코트는 문제 지도나 답안보다 강하게 보이지 않아야 하며, 장식용 이미지는 `aria-hidden`으로 처리한다.

## 6. 핵심 컴포넌트

### 버튼

- Primary는 화면당 하나이며 주요 다음 행동에 사용한다.
- Secondary는 복습, 다시 보기처럼 보조 행동에 사용한다.
- 답안 버튼은 기본, 선택, 정답, 오답 상태를 모두 가진다.
- 비활성 버튼은 색만 흐리지 말고 실제 `disabled` 상태와 명확한 대비를 제공한다.
- 방향 화살표나 꺾쇠를 버튼 안에 넣지 않는다.

### 진행 바와 상태 정보

진행 바는 현재 문제 위치를 가장 넓게 표시하고, 하트와 XP는 작은 상태 칩으로 보조한다. 콤보가 시작되면 진행 바 주변에 짧은 골드 파티클을 표시하되 문제 내용을 가리지 않는다.

### 지도 카드

서울 SVG는 원본 비율을 유지한다. 비활성 구는 중립색, 문제 구는 단청 레드, 정답은 성공 그린, 오답은 오류 레드로 표시한다. 현재 상태는 캡션과 스크린리더 라이브 영역에도 함께 전달한다.

### 피드백 패널

정답·오답 후 화면 하단에서 올라오는 패널이다. 결과 아이콘, 결과 문구, 위치 단서, `계속하기` 버튼 순으로 구성한다. 패널이 열린 뒤에도 앱인토스 닫기 기능을 가리거나 이탈을 막지 않는다.

## 7. 모션과 햅틱

| 상황 | 모션 | 기준 |
| --- | --- | --- |
| 버튼 터치 | 짧은 scale-down 후 복귀 | 입력과 동시에 반응 |
| 정답 | 피드백 패널이 아래에서 탄력 있게 등장 | 180~240ms |
| 오답 | 지도 또는 선택 답안이 짧게 좌우 흔들림 | 약 180ms, 1회 |
| 진행 | 진행 바 폭이 자연스럽게 증가 | 240~320ms |
| 3연속 정답 | 골드 파티클이 짧게 확산 | 600ms 이내 |
| 구역 해금 | 해당 자치구에 색이 차오름 | 300~500ms |
| 완료 | 마스코트와 스트릭 불꽃이 한 번 팝업 | 반복 재생 금지 |

햅틱은 지원되는 환경에서만 보조적으로 사용한다. 성공은 가벼운 탭, 오답은 짧은 경고 패턴을 사용하며 햅틱이 없어도 동일한 정보를 이해할 수 있어야 한다. `prefers-reduced-motion: reduce`에서는 흔들림, 바운스, 파티클을 제거하고 즉시 상태 전환으로 대체한다.

## 8. 게임화 규칙과 윤리

### 스트릭

하루 한 세트를 완료하면 연속 학습일이 늘어난다. 기록을 놓친 사용자를 압박하는 문구는 쓰지 않고, 재시작 경로와 주 1회 스트릭 보호권을 제공한다.

### 하트

기본 하트는 3개이며 오답 시 1개가 줄어든다. 하트가 0이 되면 결제나 광고를 요구하지 않고 오답 복습 3문제로 1개를 회복한다. 깨지는 모션은 짧고 가볍게 표현하며 실패감을 과장하지 않는다.

### 자치구 도장깨기

각 구의 학습 기준을 달성하면 흑백 지도에 해당 구의 고유 색이 채워진다. 전체 25구, 권역별 완주, 정확도 목표를 배지로 제공한다.

### XP와 리그

- 정답 +10 XP, 3연속 정답부터 콤보 +5 XP를 제공한다.
- 속도 보너스는 정확도를 해치지 않는 작은 보너스로 제한한다.
- 리그는 브론즈·실버·골드로 구성하고 참여 여부를 사용자가 선택한다.
- 순위 하락이나 마감 시간을 이용한 불안·손실 프레이밍은 사용하지 않는다.

## 9. 화면별 명세

### 홈 — 오늘의 학습 진입

핵심 정보는 “오늘 5문제를 바로 시작할 수 있다”이다.

1. 짧은 학습 약속과 타이틀
2. 기본 표정의 마스코트 또는 서울 지도 진행도
3. 스트릭, 오늘 XP, 다음 배지 현황
4. `오늘의 5문제 시작` Primary 버튼
5. 예상 소요 시간 안내

### 자치구 지도 — 수집과 스테이지 선택

핵심 정보는 “내가 익힌 구와 다음에 학습할 구”이다.

1. 전체 서울 SVG와 해금 상태
2. 완료·진행 중·잠금 범례
3. 선택한 자치구의 이름과 숙련도
4. 해당 구 학습 시작 버튼

### 퀴즈 — 문제 해결

핵심 정보는 현재 맞혀야 할 지역이다.

1. 앱인토스 플랫폼 네비게이션 아래 진행 바, 하트, XP
2. 문제 유형 라벨과 질문
3. 실제 서울 SVG, 랜드마크 사진 또는 로드뷰 중 하나의 큰 시각 자료
4. 2×2 객관식 답안 또는 한글 입력 필드
5. 선택 후 활성화되는 하단 고정 `정답 확인` 버튼

화면 안에 자체 뒤로가기·닫기 버튼을 그리지 않는다. 현재 게임 설정에서는 앱인토스가 더보기와 닫기 버튼을 제공하므로 콘텐츠가 이를 침범하지 않게 safe area를 반영한다.

### 정답·오답 피드백 — 즉시 학습

핵심 정보는 결과와 위치 단서이다.

1. 지도에 정답 구 상태 표시
2. 하단 피드백 패널과 마스코트 표정
3. `서초구는 한강 남쪽, 서울 남동부에 있어요` 같은 한 줄 단서
4. `계속하기` Primary 버튼

### 세트 완료 — 성취 정리

핵심 정보는 이번 학습으로 얻은 결과이다.

1. 완료 마스코트와 스트릭 변화
2. 획득 XP, 정확도, 정답 수
3. 새로 해금한 구 또는 배지
4. 오답이 있으면 `오답만 복습` Secondary 버튼
5. `한 세트 더` Primary 버튼

### 주간 리그 — 선택적 경쟁

핵심 정보는 현재 리그와 주간 XP이다. 내 순위를 고정 영역에 보여주되 상위 사용자를 과도하게 강조하지 않는다. 리그를 사용하지 않는 사람에게 반복적으로 참여를 요구하지 않는다.

## 10. 지도 줌과 상호작용

- 탭, 키보드 Enter·Space로 자치구를 선택할 수 있다.
- 각 자치구는 `role="button"`, 한글 `aria-label`, 포커스 링을 가진다.
- 핀치와 명시적인 확대·축소 버튼을 함께 제공하고, 확대 범위는 기본~3배로 제한한다.
- 더블 탭은 선택한 구 중심으로 확대하고, 초기화 버튼으로 전체 서울 보기에 돌아간다.
- 좌측 화면 가장자리에서 시작하는 가로 드래그는 앱인토스 뒤로가기 제스처와 충돌하지 않도록 지도 이동으로 가로채지 않는다.
- 확대 상태에서도 자치구 경계와 정답 강조가 뭉개지지 않게 SVG 벡터를 유지한다.

## 11. 접근성과 반응형 규칙

- 일반 텍스트 대비는 WCAG AA 4.5:1, 큰 텍스트는 3:1 이상을 목표로 한다.
- 정오답, 잠금, 완료 상태는 색상과 함께 아이콘·패턴·문구로 구분한다.
- 하단 고정 CTA는 `SafeAreaInsets.bottom`을 반영한다.
- 상단 콘텐츠와 컨트롤은 `SafeAreaInsets.top/right`를 반영해 플랫폼 X 버튼과 겹치지 않는다.
- 320px 폭에서도 답안과 입력이 잘리지 않아야 하며, 좁은 화면에서는 2열 답안을 1열로 전환한다.
- 지도나 사진이 CTA를 접근 불가능하게 밀어내지 않도록 세로 화면에서는 스크롤을 허용한다.
- 로딩, 지도 로드 실패와 재시도, 문제 없음, 세트 완료 상태를 각각 정의한다.

## 12. 콘텐츠 톤

- 짧고 구체적인 존댓말을 사용한다: `정답이에요!`, `한강 남쪽에 있어요.`
- 오답을 비난하지 않는다: `틀렸어요`보다 `정답은 서초구예요`를 우선한다.
- 손실을 과장하지 않는다: `스트릭을 잃기 전에` 대신 `오늘 한 세트로 이어가요`를 쓴다.
- 한 화면에서 같은 내용을 제목·배지·설명으로 반복하지 않는다.
- 사용자의 감정을 대신 단정하지 않고 확인 가능한 결과를 말한다.

## 13. 구현 우선순위

### MVP

- 홈, 5문제 퀴즈, 정오답 피드백, 완료 화면
- 실제 서울 25개 구 SVG와 객관식·주관식
- 하트, XP, 콤보, 기본 스트릭 표시
- 로딩·실패·재시도와 접근성 상태

### 다음 단계

- 지도 줌·팬과 자치구 도장깨기
- 독자 까치 마스코트와 상태 애니메이션
- 스트릭 영속화와 보호권
- 선택형 주간 리그

## 14. 성공 지표

- 첫 세트 시작률과 5문제 완주율
- 오답 뒤 다음 문제 진행률과 오답 복습률
- 7일 내 재방문율과 스트릭 유지율
- 자치구별 반복 후 정답률 변화
- 지도 확대·선택 기능의 사용률과 오터치율

빠른 정답 시간보다 반복 후 정확도와 부담 없는 재방문을 우선한다.
